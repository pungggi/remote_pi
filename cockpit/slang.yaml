base_locale: en
fallback_strategy: base_locale
input_directory: lib/i18n
input_file_pattern: .i18n.json
output_directory: lib/i18n
output_file_name: strings.g.dart
# Desktop app, sem bundle a otimizar — carrega todos os locales de cara em vez
# de import deferido (evita o load assíncrono extra no meio da troca de idioma).
lazy: false
