# Pacote .rpm do Cockpit. NÃO buildável neste Mac — gerado no CI (ubuntu-24.04
# com `apt install rpm` → rpmbuild; arch detectada por uname -m). Valida-se em
# container fedora:40 (premissa do plano).
#
# Artefato resultante: piper-cockpit-<versão>.<arch>.rpm
display_name: Piper Cockpit
summary: Cliente desktop multi-pane do Piper
license: LicenseRef-proprietary
url: https://jacobmoura.work
vendor: ngSoftware
packager: ngSoftware
packagerEmail: alessandro@pungitore.ch
group: Development/Tools
generic_name: Piper Cockpit
# Deps de runtime: GTK3 (nome do pacote no Fedora).
requires:
  - gtk3
  # Plano 46 — preview de A/V via media_kit (libmpv) usando a lib do sistema.
  - mpv-libs
  # Plano 51 — flutter_secure_storage (senha de banco) usa o Secret Service.
  - libsecret
categories:
  - Development
  - IDE
keywords:
  - pi
  - remote-pi
  - agent
  - cockpit
startup_notify: true
icon: linux/runner/resources/app_icon_512.png
metainfo: linux/ch.pungitore.piper.cockpit.metainfo.xml
description: |
  Cockpit é o cliente desktop do Piper: uma GUI multi-pane sobre o motor do
  Pi. Roda agentes (cada um um `pi --mode rpc`) e terminais lado a lado, com
  árvore de arquivos, worktrees por workspace e agendamento de daemons.
