# Pacote .deb do Cockpit. NÃO buildável neste Mac — gerado nos jobs `linux-x64`
# (ubuntu-24.04) e `linux-arm64` (ubuntu-24.04-arm) do CI. A arquitetura é
# detectada pelo Fastforge (uname -m) no runner.
#
# Artefato resultante: piper-cockpit_<versão>_<arch>.deb
display_name: Piper Cockpit
package_name: piper-cockpit
# Installed-Size do pacote .deb, em KB (campo obrigatório do maker do Fastforge).
# Estimativa do bundle Flutter Linux release (~150 MB); recalibrar com
# `du -sk build/linux/*/release/bundle` quando houver build Linux real.
installed_size: 150000
maintainer:
  name: ngSoftware
  email: alessandro@pungitore.ch
section: devel
priority: optional
# Deps de runtime: GTK3 + libs base do bundle Flutter Linux. (Confirmar/expandir
# via `ldd` do bundle no CI — premissa do plano.)
dependencies:
  - libgtk-3-0
  - libblkid1
  - liblzma5
  # Plano 46 — preview de A/V via media_kit (libmpv). No Linux usamos a libmpv
  # do sistema (em macOS/Windows é embarcada pelo media_kit_libs_video).
  - libmpv2
  # Plano 51 — flutter_secure_storage (senha de banco) usa o Secret Service.
  - libsecret-1-0
generic_name: Piper Cockpit
categories:
  - Development
  - IDE
keywords:
  - pi
  - piper
  - agent
  - cockpit
startup_notify: true
startup_wm_class: ch.pungitore.piper.cockpit
icon: linux/runner/resources/app_icon_512.png
metainfo: linux/ch.pungitore.piper.cockpit.metainfo.xml
